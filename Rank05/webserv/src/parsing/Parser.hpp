#ifndef PARSER_HPP
#define PARSER_HPP

class Parser
{
private:


public:
        Parser();
        Parser(const Parser& );
        Parser& operator=(const Parser& );
        ~Parser(void);
};

#endif // PARSER_HPP